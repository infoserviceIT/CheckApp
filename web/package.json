{
  "name": "checkapp-web",
  "version": "0.1.0",
  "private": true,
  "description": "Anonymous upload-and-scan web portal for CheckApp. A thin Express wrapper around the checkapp library in ../src — all the actual rule logic lives there, this just adds an HTTP front door.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.21.2",
    "express-rate-limit": "^7.4.1",
    "multer": "^2.2.0"
  }
}
